# AI智能组卷功能说明

## 功能概述

AI智能组卷功能允许用户通过配置试卷结构，让AI自动生成题目并组成完整的试卷。该功能集成了并发控制、成本控制和用户体验优化。

## 主要特性

### 1. 智能生成
- 根据用户配置的题型、难度、关键词自动生成题目
- 支持单选题、多选题、编程题三种题型
- 支持简单、中等、困难三种难度级别
- 支持Go、JavaScript、Java、Python、C++五种编程语言

### 2. 并发控制
- 最大并发数：3个请求同时进行
- 批量生成：每批最多5道题，减少单次请求压力
- 智能调度：自动管理并发任务，避免API限流

> 生成速度
>
> - 10道题：30秒 - 1分钟
> - 30道题：1分钟 - 2分钟
> - 50道题：2分钟 - 3分钟

### 3. 成本控制
- 单次生成限制：最多100道题目
- 批次优化：合理分配生成任务，降低API调用成本
- 错误处理：生成失败时及时停止，避免浪费资源

### 4. 用户体验
- 实时进度反馈：显示当前生成进度
- 灵活配置：支持多个大题，每个大题独立配置
- 自动保存：生成的题目自动保存到题库
- 一键预览：生成完成后自动跳转到试卷预览

## 实现细节

### 并发控制实现

```go
// 使用信号量控制并发
semaphore := make(chan struct{}, maxConcurrency)

// 获取信号量
semaphore <- struct{}{}
defer func() { <-semaphore }()

// 执行任务
```

### 批量生成实现

```go
// 分批生成
batchSize := 5
for i := 0; i < section.Count; i += batchSize {
    currentBatchSize := min(batchSize, section.Count - i)
    // 生成当前批次
}
```

### 错误处理实现

```go
// 使用互斥锁保护错误变量
var errMutex sync.Mutex
var firstError error

// 检查错误
errMutex.Lock()
if firstError != nil {
    errMutex.Unlock()
    break
}
errMutex.Unlock()
```

## 性能指标

### 生成速度

- 10道题：30秒 - 1分钟
- 30道题：1分钟 - 2分钟
- 50道题：2分钟 - 3分钟

## 技术架构

### 后端架构

```
┌─────────────────────────────────────────┐
│           API Layer (handler.go)        │
│  - 接收HTTP请求                          │
│  - 参数验证                              │
│  - 调用服务层                            │
└─────────────────┬───────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│      Service Layer (ai_paper_service)   │
│  - AI生成逻辑                            │
│  - 并发控制                              │
│  - 批量生成                              │
│  - 错误处理                              │
└─────────────────┬───────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│         Store Layer (db.go)             │
│  - 数据库操作                            │
│  - 事务管理                              │
│  - 数据持久化                            │
└─────────────────────────────────────────┘
```

### 前端架构

```
┌─────────────────────────────────────────┐
│    Page (PaperManagePage.tsx)          │
│  - 入口按钮                              │
│  - 状态管理                              │
│  - 回调处理                              │
└─────────────────┬───────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│  Component (AIGeneratePaperModal.tsx)   │
│  - 配置界面                              │
│  - 表单验证                              │
│  - 动态配置                              │
│  - 实时计算                              │
└─────────────────┬───────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│         API Layer (api/index.ts)        │
│  - HTTP请求                              │
│  - 数据转换                              │
│  - 错误处理                              │
└─────────────────────────────────────────┘
```

## 使用方法

### 前端操作

1. **进入试卷管理页面**
   - 点击导航栏的"试卷管理"

2. **打开AI组卷对话框**
   - 点击页面右上角的"AI智能组卷"按钮（紫色按钮）

3. **配置试卷信息**
   - 填写试卷标题（必填）
   - 配置大题信息：
     - 大题名称：如"一、单选题"
     - 题型：单选题/多选题/编程题
     - 题目数量：1-50道
     - 每题分数：1-100分
     - 难度：简单/中等/困难
     - 编程语言：Go/JavaScript/Java/Python/C++
     - 关键词：如"Go语言基础"（必填）

4. **添加更多大题**
   - 点击"添加大题"按钮可以添加更多大题
   - 点击大题卡片右上角的"删除"按钮可以删除大题（至少保留一个）

5. **开始生成**
   - 检查总题数和总分
   - 点击"开始生成"按钮
   - 等待1-3分钟（根据题目数量）
   - 生成完成后自动跳转到试卷预览页面

### API接口

**端点**: `POST /api/papers/ai-generate`

**请求体**:
```json
{
  "title": "Go语言期末考试试卷",
  "sections": [
    {
      "name": "一、单选题",
      "type": 1,
      "count": 10,
      "score_each": 2,
      "difficulty": 1,
      "language": "go",
      "keyword": "Go语言基础"
    },
    {
      "name": "二、多选题",
      "type": 2,
      "count": 5,
      "score_each": 4,
      "difficulty": 2,
      "language": "go",
      "keyword": "Go并发编程"
    }
  ]
}
```

**响应**:
```json
{
  "code": 0,
  "msg": "AI生成试卷成功",
  "data": {
    "id": 1,
    "title": "Go语言期末考试试卷",
    "config": [...],
    "created_at": "2025-10-21T10:30:00Z"
  }
}
```

## 技术实现

### 后端架构

1. **服务层** (`service/ai_paper_service.go`)
   - `AIGeneratePaper`: 核心生成函数，支持上下文取消
   - `QuickAIGeneratePaper`: 简化版生成函数
   - 并发控制：使用信号量限制最大并发数
   - 批量生成：将大题拆分为多个批次
   - 错误处理：任何批次失败立即停止

2. **API层** (`api/handler.go`)
   - `AIGeneratePaper`: 处理HTTP请求
   - 参数验证
   - 调用服务层
   - 返回结果

3. **路由层** (`router/router.go`)
   - 注册路由：`POST /api/papers/ai-generate`

### 前端架构

1. **组件** (`components/AIGeneratePaperModal.tsx`)
   - 对话框组件
   - 表单验证
   - 动态大题配置
   - 实时计算总分和总题数

2. **页面集成** (`pages/PaperManagePage.tsx`)
   - 添加入口按钮
   - 处理生成成功回调
   - 自动刷新列表
   - 跳转到预览页面
